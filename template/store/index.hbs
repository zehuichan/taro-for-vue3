import { defineStore } from 'pinia'
import { store } from '../index'

export const use{{pascalCase name}}Store = defineStore({
  id: '{{name}}',
  state: () => ({}),
  getters: {},
  actions: {}
})

export function use{{pascalCase name}}StoreWithInstall() {
  return use{{pascalCase name}}Store(store)
}